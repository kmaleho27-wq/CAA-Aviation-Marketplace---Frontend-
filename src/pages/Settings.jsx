import PageStub from '../components/PageStub';

export default function Settings() {
  return (
    <PageStub
      overline="Account"
      title="Settings"
      phase="later"
      description="Profile, organisation details, notification preferences, API keys and team management. Coming after the core flows ship."
    />
  );
}
